---
status: proposed
tags: [adr]
---

# ADR-0000 — Short title (YYYY-MM-DD)

Copy this file to `NNNN-short-title.md` (next number). Fill in, flip
`status:` to `accepted`. Never edit after acceptance — a reversal is a
new ADR, plus flip this file's `status:` to `superseded by [[NNNN-...]]`.

- Context: (forcing error, constraint, or new info — 2–3 lines max)
- Decision: (what changes; plain IDs only, e.g. FR-2.3, C-5, DATA_MODEL §3)
- Consequences: (TASKS boxes / code / notes to update; what is now forbidden)
